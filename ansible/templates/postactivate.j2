#!/bin/bash
# virtualenv postactivate script

PS1="[pk] $_OLD_VIRTUAL_PS1"
export PATH="$PATH:{{project_dir}}/node_modules/.bin"
export PYTHONPATH="{{root_dir}}:$PYTHONPATH"
export DJANGO_SETTINGS_MODULE="pk.settings.settings"
if [ -f {{virtualenv}}/src/django/extras/django_bash_completion ]
  then source {{virtualenv}}/src/django/extras/django_bash_completion
fi
